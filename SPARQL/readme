\name{AIDGO}
\title{AIDGO Ontology Pipeline Working Manual}
\description{
AIDGO (AI Data Governance Ontology) is a semantic alignment pipeline for linking 
legal requirements from the EU AI Act (Article 10) with technical clauses from 
ISO/IEC 5259 (Data Quality for Analytics & AI). 

This documentation describes the components, data structures, scripts, and usage 
instructions for generating an RDF-Star ontology for regulatory-technical alignment.
}

\section{Repository Structure}{
\preformatted{
data/
  article10_requirements.csv
  iso5259_clauses.csv
  aidgo_alignment.csv

scripts/
  merge_to_aidgo_rdfstar.py
  suggest_alignments.py

output/
  aidgo_output.ttl

examples/
  example_alignment.ttl
  example_queries.rq
}
}

\section{Installation}{
Requires Python 3.9+ and the following packages:
- rdflib (>= 6.2, RDF-Star support)
- pandas
- scikit-learn

Install via:
\preformatted{
pip install rdflib pandas scikit-learn
}
}

\section{Input Data Files}{
Three input datasets are required.

\subsection{article10_requirements.csv}{
Contains atomic requirements extracted from Article 10 of the EU AI Act.

Columns:
\itemize{
  \item RequirementID
  \item skos:prefLabel
  \item skos:definition
}
}

\subsection{iso5259_clauses.csv}{
Contains atomic clauses extracted from ISO/IEC 5259 standards.

Columns:
\itemize{
  \item ClauseID
  \item skos:prefLabel
  \item skos:definition
}
}

\subsection{aidgo_alignment.csv}{
Defines semantic alignment between Article 10 requirements and ISO clauses.

Columns:
\itemize{
  \item Requirement
  \item Clause
  \item AlignmentType
  \item Rationale
  \item skos:prefLabel (optional)
  \item skos:definition (optional)
}
}
}

\section{Generating the RDF-Star Ontology}{
Use the main script:
\preformatted{
python scripts/merge_to_aidgo_rdfstar.py
}

This produces:
\preformatted{
output/aidgo_output.ttl
}

The output includes:
\itemize{
  \item RegulatoryRequirement instances
  \item TechnicalClause instances
  \item Alignment nodes
  \item RDF-Star embedded rationale triples
}
}

\section{SPARQL Query Examples}{
Queries in RDF-Star format are included in:
\preformatted{
examples/aidgo_queries.rq
}

Example:
\preformatted{
PREFIX aidgo: <http://example.org/aidgo#>

SELECT ?requirement ?clause ?type ?rationale
WHERE {
  ?align a aidgo:Alignment ;
         aidgo:regulatoryRequirement ?requirement ;
         aidgo:technicalClause ?clause ;
         aidgo:alignmentType ?type .

  << ?align aidgo:alignmentType ?type >>
      aidgo:rationale ?rationale .
}
}
}

\section{Alignment Suggestion Tool}{
Use TF-IDF similarity to identify likely mappings:

\preformatted{
python scripts/suggest_alignments.py
}

This generates:
\preformatted{
data/alignment_suggestions.csv
}
}

\section{Troubleshooting}{
\itemize{
  \item Empty TTL file: check CSV column names.
  \item RDF-Star missing: ensure rdflib >= 6.2 is installed.
  \item AlignmentType errors: must match expected vocabulary exactly.
}
}

\section{Citation}{
If using this work in academic publications, cite:

Aiyankovil, K. G. (2025).
AIDGO: A Semantic Alignment Framework for AI Act Data Governance Requirements 
and ISO/IEC 5259.
}
